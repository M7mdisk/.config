alias vim="nvim"
alias v="vim"

alias ls="ls --color=auto"
alias l="ls"
alias ll="ls -la"

# Git stuff
alias lg="lazygit"
alias g="git"
alias gs="git status"
alias gc="git commit -m"
alias gl="git log"

# Tmux
alias t="tmux -u"
alias tls="tmux list-sessions"
alias ta="tmux -u attach"

# WSL Stuff
alias c="cd /mnt/c"
alias d="cd /mnt/d"

alias i="sudo xbps-install -S"
alias r="sudo xbps-install -R"
alias q="sudo xbps-query -Rs"
alias u="sudo xbps-install -u; sudo xbps-install -Su"
